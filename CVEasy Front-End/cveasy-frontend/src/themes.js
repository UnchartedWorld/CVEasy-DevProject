import {createTheme} from "@mui/material/styles";


const themes = createTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#77a978',
        },
        secondary: {
            main: '#b70747',
        },
    },
});

export default themes